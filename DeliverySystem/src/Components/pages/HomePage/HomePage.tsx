import { Grid2, Typography } from '@mui/material';

export const HomePage = () => {
  return (
    <Grid2 mt={1}>
      <Typography p={10} textAlign={'center'}>This is a Homepage</Typography>
    </Grid2>
  );
}